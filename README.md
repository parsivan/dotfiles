# How to Use
for configuration files in .config simply use:
```stow <app/folder name>```
```stow waybar```
for configuration files in /etc use:
```sudo stow -t / <app/foldername>```
```sudo stow -t / stubby```
